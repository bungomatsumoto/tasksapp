<h3><%= t('view.edit') %></h3>

<%= render "form" %>

<p class="to_other_page"><%= link_to t('view.show'), task_path(@task) %></p>
<p class="to_other_page"><%= link_to t('view.index'), tasks_path %></p>
